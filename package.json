{
  "name": "pdsa-points-system",
  "version": "0.1.0",
  "private": true,
  "description": "PDSA UCF points system: Postgres schema, requirements engine, RPCs and RLS.",
  "type": "module",
  "engines": {
    "node": ">=20.11"
  },
  "scripts": {
    "test": "node --test 'test/*.test.mjs'",
    "lint:no-em-dash": "bash scripts/check_no_em_dash.sh",
    "check": "npm run lint:no-em-dash && npm test"
  },
  "devDependencies": {
    "@electric-sql/pglite": "^0.5.4",
    "jsqr": "^1.4.0"
  }
}
